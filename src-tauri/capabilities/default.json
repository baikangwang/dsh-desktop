{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window (custom commands + core invoke)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
